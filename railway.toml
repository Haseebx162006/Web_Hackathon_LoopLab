[build]
builder = "NIXPACKS"
buildCommand = "npm ci --prefix backend/app"

[deploy]
startCommand = "npm --prefix backend/app start"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10